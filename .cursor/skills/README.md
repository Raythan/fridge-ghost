# Skills do projeto Pra Já (fridge-ghost)

Cada pasta contém um `SKILL.md` com frontmatter (`name`, `description`). O agente usa a **description** para decidir quando ler a skill.

| Skill | Papel (especialista) |
|-------|----------------------|
| `fridge-ghost-architecture` | Arquiteto de solução / tech lead |
| `fridge-ghost-ui-dom` | Frontend DOM + Tailwind + pt-BR |
| `fridge-ghost-security` | AppSec (cliente estático, dados, XSS) |
| `fridge-ghost-clean-code` | Staff engineer / manutenibilidade |
| `fridge-ghost-performance` | Frontend performance |
| `fridge-ghost-scalability` | Arquiteto de software (crescimento) |
| `fridge-ghost-lean-code` | Principal engineer (mínimo necessário) |
| `fridge-ghost-repository-guardrails` | Release engineer / integridade Git |

Regra global complementar: `.cursor/rules/fridge-ghost-repo-safety.mdc` (`alwaysApply`).
